<Window x:Class="MyRPCClientGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyRPCClientGUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="400">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"></ColumnDefinition>
            <ColumnDefinition Width="150"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="账号:" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBox x:Name="Tb_Account" Grid.Column="1"  VerticalAlignment="Center"/>
        <TextBlock Text="密码:" Grid.Row="1"  HorizontalAlignment="Right" VerticalAlignment="Center"/>
        <TextBox x:Name="Tb_Password" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center"/>
        <Button Content="登陆" Grid.ColumnSpan="2" Grid.Row="2" Width="100" Height="30" Margin="10" Click="Button_Click"/>
    </Grid>
</Window>
